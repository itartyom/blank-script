# Минимальный сетап для скрипта

- pyproject.toml
- justfile с определенным run
- энтрипоинт main.py
- .env пишется при установке, для остального используем config.toml

## Использование

```shell
# Сначала сделай just init, он создаст config.toml
just init
```